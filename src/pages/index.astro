---
/**
 * 목적: 홈 그리드 페이지. index.json을 로드하여 카드 리스트/검색/카테고리 필터를 제공한다.
 */
import '../styles/global.css';
import Card from '../../src/components/Card.astro';
const SITE_TITLE = 'AI 뉴스 보드';
// Vite 번들에 포함시키기 위해 클라이언트 스크립트를 URL로 import
// 외부 모듈 스크립트를 직접 참조하면 Vite가 번들/경로를 자동 처리한다.
// 번들 시 경로 문제가 없도록 Vite `?url`을 사용해 스크립트 URL을 주입한다.
// 외부 모듈 스크립트 경로 이슈를 피하기 위해 이 페이지에서 직접 초기화 스크립트를 실행한다.
---
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{SITE_TITLE}</title>
    <link rel="icon" href={import.meta.env.BASE_URL + 'favicon.svg'} type="image/svg+xml" />
    <link rel="icon" href={import.meta.env.BASE_URL + 'favicon-32x32.png'} sizes="32x32" />
    <link rel="apple-touch-icon" href={import.meta.env.BASE_URL + 'apple-touch-icon.png'} sizes="180x180" />
    <meta name="app-base" content={import.meta.env.BASE_URL} />
  </head>
  <body>
    <header>
      <div class="container" style="display:flex;gap:12px;align-items:center;justify-content:space-between;">
        <strong>{SITE_TITLE}</strong>
        <input id="q" class="searchbar" placeholder="검색: 제목, 분류, 원문…" aria-label="전역 검색" />
      </div>
      <div class="container">
        <div id="tabs" class="tabs" role="tablist" aria-label="카테고리 필터"></div>
      </div>
    </header>
    <main class="container">
      <div id="grid" class="grid" aria-live="polite"></div>
    </main>

    <script type="module" src={import.meta.env.BASE_URL + 'home.js'}></script>
  </body>
  </html>


